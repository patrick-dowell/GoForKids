export { Board } from './Board';
export { Game } from './Game';
export {
  BOARD_SIZE,
  Color,
  oppositeColor,
  MoveResult,
  pointToIndex,
  indexToPoint,
  pointsEqual,
  isValidPoint,
  neighbors,
} from './types';
export type { Point, MoveRecord, GameResult } from './types';
