# b28 — production calibration for the b28c512nbt KataGo network.
#
# Calibrated 2026-05-03/04 against the b20 baseline (g170e-b20c256x2) using
# data/calibrate_b28.py — head-to-head matches on each (rank, board_size)
# pair, knobs adjusted until b28-side win rate landed in the 45-55% target
# band (or as close as the profile permitted; per-profile iteration history
# in the inline comments below).
#
# Each profile's header comment lists the iterations tried and the final
# locked rate / margin. The dominant pattern: at moderate-to-high visit
# counts on bigger boards (9x9 6k, all 13x13 mid+high ranks, all 19x19),
# b28's stronger network dominates the symmetric profile and "heavy noise"
# (visits cut + mistake_freq bumped + random_move_chance bumped + max_pl
# capped) was needed to pull the rate back toward 50%. Some profiles —
# notably 13x13 15k — couldn't be brought into band with profile knobs at
# all and were locked at the b20-clone with documented over-strength.
#
# Same lookup semantics as b20.yaml (see profile_loader.py for details).

profiles:
  19x19:
    "30k":
      # b28 calibration:
      #   v1 visits=10 max_pl=30 mistake=0.55 rand=0.08: 90.0% n=30 margin +167.90
      #   v2 visits=10 max_pl=18 mistake=0.55 rand=0.08: 86.7% n=30 margin +137.43
      # max_pl reduction barely moved the needle (90% -> 87%). 19x19 30k is the
      # weakest-tier "Seedling" bot — kids' first 19x19 experience — and a 87%
      # rate with +137 margin means total domination. v3: aggressive multi-knob
      # noise injection to neutralize b28's MCTS quality.
      #   mistake_freq 0.55 -> 0.75 (more deliberate suboptimal moves)
      #   random_move_chance 0.08 -> 0.20 (more pure-random moves)
      #   visits 10 -> 6 (shallow search reduces b28's MCTS edge, though this
      #     theory failed on 13x13; trying anyway for the Seedling profile).
      # v3 dropped the b28 win rate from 90% to 67% but still over the 45-55%
      # target band, and playtesting confirmed it feels too strong (and barely
      # weaker than 19x19 18k — both ended up on the same v3 template).
      # v4 (2026-05-13): port the "weakening" knobs that smaller-board 30k
      # profiles use but 19x19 30k was missing — strong local_bias to distract
      # the bot from globally optimal play, clarity_prior + clarity_score_gap
      # so it concedes settled positions, pass_threshold, bigger max_point_loss
      # so individual mistakes hurt more.
      #   local_bias 0.42 -> 0.80 + local_bias_in_opening: true
      #   max_point_loss 18 -> 28 (between 13x13's 22 and 9x9's 30)
      #   add clarity_prior 1.1, clarity_score_gap 999.0, pass_threshold 0.15
      # v4 verdict (2026-05-13): tested 30k+H9 vs 18k on 19x19 (asymmetric
      # handicap harness, b28+b28.yaml both sides) — 4/4 sweep for 30k+H9
      # with avg margin +347. Tried v5 with much more aggressive weakening
      # (visits 6->3, mistake_freq 0.75->0.88, rand 0.20->0.40, opening_moves
      # 8->2) and the margin got WORSE (+385 over 2 games), confirming the
      # cross-rank handicap bot-vs-bot test is structurally broken — the
      # defender's noise is cheap (local-bias plays roughly correct moves
      # for handicap defense), the attacker's noise is catastrophic. Rolled
      # back to v4 for human-playtest validation. v4 keeps v3's noise levels
      # (visits/mistake_freq/random_move_chance) but adds clarity-based
      # passing + strong local_bias + bigger max_pl so the bot stops grinding
      # through the endgame in clearly-decided positions.
      max_point_loss: 28.0
      mistake_freq: 0.75
      policy_weight: 0.15
      randomness: 0.78
      random_move_chance: 0.20
      local_bias: 0.80
      local_bias_in_opening: true
      first_line_chance: 0.0
      visits: 6
      min_candidates: 15
      opening_moves: 8
      pass_threshold: 0.15
      clarity_prior: 1.1
      clarity_score_gap: 999.0
    "18k":
      # b28 calibration round 1: b20-clone landed at 80% n=30, margin +137.23.
      # Applying the 19x19 30k v3 heavy-noise template (proven to drop 30k from
      # 90% to 67%): cut visits 12->6, bump mistake_freq 0.55->0.72, bump
      # random_move_chance 0.12->0.22, cap max_point_loss 28->18.
      max_point_loss: 18.0
      mistake_freq: 0.72
      policy_weight: 0.18
      randomness: 0.76
      random_move_chance: 0.22
      local_bias: 0.38
      first_line_chance: 0.0
      visits: 6
      min_candidates: 15
      opening_moves: 12
    "15k":
      # b28 calibration round 1: b20-clone (visits=30, mistake=0.40) landed at
      # 83.3% n=30, margin +144. v2: heavy-noise template — visits 30->8,
      # mistake 0.40->0.65, rand 0.05->0.18, max_pl 20->14.
      max_point_loss: 14.0
      mistake_freq: 0.65
      policy_weight: 0.30
      randomness: 0.60
      random_move_chance: 0.18
      local_bias: 0.25
      first_line_chance: 0.0
      visits: 8
      min_candidates: 12
      opening_moves: 30
    "12k":
      # b28 calibration: v1 b20-clone visits=42 -> 90%/+165. v2 heavy-noise
      # template (worked at 19x19 30k/15k): visits 42->8, mistake 0.34->0.55,
      # rand 0.02->0.15, max_pl 17->13.
      max_point_loss: 13.0
      mistake_freq: 0.55
      policy_weight: 0.42
      randomness: 0.45
      random_move_chance: 0.15
      local_bias: 0.20
      first_line_chance: 0.0
      visits: 8
      min_candidates: 10
      opening_moves: 22
    "9k":
      # 2026-05-05 RELABEL — interpolated midway between 12k Stream and the
      # old (over-strong) 9k profile. First-pass strength was too low: 8x10
      # bot-vs-bot trial showed 12k vs new-9k at only 60/40 even (target
      # ~85% for 3-rank gap on b28), 12k+H3 vs new-9k at 60/40 (target 50%).
      # Strength gap to 12k was ~1 rank, not 3.
      # Root cause: profile knobs aren't linear in strength. Visit-count →
      # strength curve is steep around visits 10-12 (where 6k vs 3k showed
      # 90% even), so the midway visits=9 sat much closer to 12k(visits=8)
      # than to 6k(visits=10) in skill-space.
      # Bump pass: shift the strength levers (visits, max_pl, rand) toward
      # the 6k slot's values; keep mistake_freq slightly above 6k's so the
      # 9k → 6k gap stays meaningful.
      # Validated 2026-05-05 in a 60-game v3 trial — both adjacent H3 matches
      # landed at 50% (12k+H3 vs 9k = 5/10, 9k+H3 vs 6k = 5/10), the textbook
      # 3-rank-gap target. Even-game numbers stay noisy (60% vs 12k, 90% vs
      # 6k) — H3 is the trustworthy signal at low visit counts on b28.
      # Trial logs: data/calibration_logs_b28/relabel_trial_2026-05-05/
      max_point_loss: 9.0
      mistake_freq: 0.51
      policy_weight: 0.47
      randomness: 0.41
      random_move_chance: 0.12
      local_bias: 0.15
      first_line_chance: 0.0
      visits: 10
      min_candidates: 9
      opening_moves: 21
    "6k":
      # 2026-05-05 RELABEL — was 19x19 9k. Original calibration:
      # v1 b20-clone (visits=80, mistake=0.25) -> 86.7%/+119.
      # v2 heavy-noise: visits 80->10, mistake 0.25->0.50, rand 0.02->0.12,
      # max_pl 10->8. Playtested as ~6k strength against humans, hence the
      # ladder shift.
      # v3 2026-06-11 soften (playtest: "a little too hard" at rungs 8k-6k):
      # mistake_freq 0.50->0.58, max_pl 8->6 — MORE mistakes but SMALLER, per
      # Patrick's direction. Two knobs on the SAME axis (mistake character),
      # quality knobs (visits/policy_weight) untouched. NOTE: lands alongside
      # the 2026-06-11 settle/komi fix, which STOPS the bot giving away 2-3
      # pts/game filling its own territory — net target is "slightly easier
      # than before", so the soften must outweigh that gain.
      # ⚠️ UNVALIDATED — verify via bot-vs-bot (6k should still beat 9k and
      # lose to 3k) or playtest; if overshot, revert max_pl 6->8 first.
      max_point_loss: 6.0
      mistake_freq: 0.58
      policy_weight: 0.50
      randomness: 0.40
      random_move_chance: 0.12
      local_bias: 0.12
      first_line_chance: 0.0
      visits: 10
      min_candidates: 8
      opening_moves: 20
    "3k":
      # 2026-05-05 RELABEL — was 19x19 6k. Original calibration:
      # v1 b20-clone (visits=120, mistake=0.22) -> 86.7%/+80.
      # v2 heavy-noise: visits 120->12, mistake 0.22->0.45, rand 0.02->0.10,
      # max_pl 9->7. Playtested as ~3k strength against humans, hence the
      # ladder shift. Replaces the old interpolated 3k placeholder.
      max_point_loss: 7.0
      mistake_freq: 0.45
      policy_weight: 0.50
      randomness: 0.38
      random_move_chance: 0.10
      local_bias: 0.08
      first_line_chance: 0.0
      visits: 12
      min_candidates: 8
      opening_moves: 18
    "1d":
      # b28 calibration: v1 b20-clone (visits=300, mistake=0.06) -> 83.3%/+57.
      # v2 moderate noise (1d profile starts tight; less aggressive than mid ranks):
      # visits 300->30, mistake 0.06->0.22, rand 0.0->0.05, max_pl 2.5->4.5.
      max_point_loss: 4.5
      mistake_freq: 0.22
      policy_weight: 0.85
      randomness: 0.10
      random_move_chance: 0.05
      local_bias: 0.0
      first_line_chance: 0.0
      visits: 30
      min_candidates: 5
      opening_moves: 5

  13x13:
    "30k":
      # b28 calibration:
      #   v1 visits=5  max_pl=35: 33.3% n=30 margin -18.67
      #   v2 visits=8  max_pl=35: 30.0% n=30 margin -34.77 (worse — visits
      #     not the lever; combined 19/60 = 31.7%)
      #   v3 (current): max_point_loss 35 -> 22, visits back to 5. Hypothesis:
      #     b28's sharper score_lead estimates mean the mistake mechanism
      #     (which picks moves up to max_point_loss worse) actually loses
      #     close to that many points on b28-side, while b20's noisier
      #     estimates yielded smaller real losses for the "same" mistake.
      max_point_loss: 22.0
      mistake_freq: 0.65
      policy_weight: 0.10
      randomness: 0.85
      random_move_chance: 0.15
      local_bias: 0.80
      local_bias_in_opening: true
      first_line_chance: 0.0
      visits: 5
      min_candidates: 12
      opening_moves: 4
      pass_threshold: 0.15
      clarity_prior: 1.1
      clarity_score_gap: 999.0
    "15k":
      # b28 calibration: 5 rounds explored visits + mistake_freq + max_pl axes.
      # All rounds cluster 70-83% (combined 115/150 = 76.7%). b28's MCTS quality
      # at this rank is robust to all knob changes attempted; profile-symmetric
      # tweaks can't bridge the strength gap on 13x13.
      #   v1 visits=18 mistake=0.42 max_pl=22: 70.0% n=30, margin +35.70
      #   v2 visits=12 mistake=0.55 max_pl=22: 76.7% n=30, margin +51.57
      #   v3 visits=18 mistake=0.55 max_pl=30: 80.0% n=30, margin +54.77
      #   v4 visits=6  mistake=0.42 max_pl=22: 83.3% n=30, margin +53.93
      #   v5 visits=40 mistake=0.42 max_pl=22: 73.3% n=30, margin +31.43
      # LOCKED at v1 (b20-clone, simplest, tied with v5 for best rate). Bot
      # will play stronger than nominal 15k — kids picking 13x13 15k face a
      # ~12k-equivalent. Rank ordering is preserved (30k < 15k < 6k strength).
      max_point_loss: 22.0
      mistake_freq: 0.42
      policy_weight: 0.28
      randomness: 0.62
      random_move_chance: 0.06
      local_bias: 0.22
      first_line_chance: 0.0
      visits: 18
      min_candidates: 10
      opening_moves: 8
      pass_threshold: 0.15
    "6k":
      # b28 calibration round 1: b20-clone (visits=70, mistake=0.22) landed at
      # 73.3% n=30 (CI 55.6-85.8%, margin +28.80). Mirrors 13x13 15k pattern.
      # v2: bump mistake_freq 0.22 -> 0.45 (the lever that helped at 9x9 6k).
      max_point_loss: 9.0
      mistake_freq: 0.45
      policy_weight: 0.50
      randomness: 0.38
      random_move_chance: 0.02
      local_bias: 0.08
      first_line_chance: 0.0
      visits: 70
      min_candidates: 8
      opening_moves: 6
      pass_threshold: 0.15

  9x9:
    # FULL DATA-CALIBRATED LADDER — S42 distribution campaign, 2026-07-05.
    # Every rung 18k→1d is a reading_rate/policy_temp sampling profile,
    # placed against the HUMAN per-move loss table (OGS reference games in
    # data/human_games_9x9_ogs/, analyzer: data/analyze_9x9_losses.py).
    # Scale is OGS-flavored (Patrick, IGS 2d, measures above the OGS 1d
    # column). Evidence per rung below; full history: DEVJOURNAL S38-S42,
    # data/profiles/9x9_profile_archive.yaml, and the complete game log in
    # data/calibration_logs_b28/9x9_s38-s42_bot_battles.md.
    # Rank dial: reading_rate ↑, policy_temp ↓, random_move_chance ↓,
    # wide_root_noise ↓ as rank rises. mistake_freq/max_point_loss only
    # touch the (small) reading fraction below 3k.
    # ROUND 2 (2026-07-05 late): Patrick's full-ladder pass froze 30k/18k/
    # 6k/1d as anchors and convicted the middle — the rr dial barely dialed
    # (tactical free-riding: big prior gaps survive any temperature), so
    # 15k/12k were erratic-strong and 3k reached 1d-minus. Sampler v2 adds
    # sample_lapse (miss the vital point) + sample_loss_cap (misses cost
    # points, not games) on 15k/12k/9k/3k. PENDING: round-2 bot battles +
    # phase curves, then Patrick's next device pass.
    "30k":
      # Playtest-validated pure-heuristic anchor (2026-05-18) — UNCHANGED.
      # Do not retune without explicit playtest confirmation.
      max_point_loss: 30.0
      mistake_freq: 0.70
      policy_weight: 0.08
      randomness: 0.88
      random_move_chance: 0.20
      local_bias: 0.85
      local_bias_in_opening: true
      first_line_chance: 0.0
      visits: 4
      min_candidates: 10
      opening_moves: 2
      pass_threshold: 0.10
      clarity_prior: 1.1
      clarity_score_gap: 999.0
    "18k":
      # Target: human 18k column (31% blunders, median 1.78, mean 6.1).
      # Histogram pending; bot-vs-bot can't resolve ranks this weak
      # (mutual-weakness compression) — kid/tester feel is the instrument.
      max_point_loss: 30.0
      mistake_freq: 0.80
      policy_weight: 0.12
      randomness: 0.78
      random_move_chance: 0.30
      local_bias: 0.0
      local_bias_in_opening: false
      local_bias_from_candidates: false
      reading_rate: 0.03
      policy_temp: 3.5
      wide_root_noise: 0.8
      first_line_chance: 0.0
      visits: 16
      min_candidates: 10
      opening_moves: 3
      pass_threshold: 0.10
      clarity_prior: 0.95
      clarity_score_gap: 25.0
    "15k":
      # Target: human 15k column (median 1.11, near-opt 39%, blunder 16%).
      max_point_loss: 28.0
      mistake_freq: 0.75
      policy_weight: 0.12
      randomness: 0.78
      random_move_chance: 0.1
      local_bias: 0.0
      local_bias_in_opening: false
      local_bias_from_candidates: false
      reading_rate: 0.05
      policy_temp: 2.6
      wide_root_noise: 0.7
      sample_lapse: 0.45
      sample_loss_cap: 7.0
      first_line_chance: 0.0
      visits: 16
      min_candidates: 10
      opening_moves: 3
      pass_threshold: 0.10
      clarity_prior: 0.90
      clarity_score_gap: 18.0
    "12k":
      # S50c (2026-07-06): re-seated. The old 12k profile moved up to the
      # 9k seat (Patrick validated it there by feel); this rung is now the
      # knob-midpoint of its neighbors 15k and (new) 9k. Untested at birth
      # by design — Patrick device-tests before any automated runs.
      max_point_loss: 27.0
      mistake_freq: 0.74
      policy_weight: 0.12
      randomness: 0.78
      random_move_chance: 0.08
      local_bias: 0.0
      local_bias_in_opening: false
      local_bias_from_candidates: false
      reading_rate: 0.065
      policy_temp: 2.45
      wide_root_noise: 0.7
      sample_lapse: 0.40
      sample_loss_cap: 6.5
      first_line_chance: 0.0
      visits: 16
      min_candidates: 10
      opening_moves: 3
      pass_threshold: 0.10
      clarity_prior: 0.885
      clarity_score_gap: 16.5
    "9k":
      # S50c (2026-07-06, Patrick's call): the new Boulder = the old 12k
      # profile VERBATIM. He beats that profile 3/4 and rates it "very
      # beatable with a touch of 9k strength" — after a week of failed
      # weakenings of the old 9k lineage (device-validated too strong every
      # round: 5QDYHMFG, 2J77PPVC, N76NV5W6), the validated bot takes the
      # 9k seat. 6k stays as the anchor above (gap size to be felt out).
      # The sampler-v3 knobs (sample_min_loss, read_cooldown) are NOT used
      # here — that mechanism lives in both selectors, dormant, for future
      # texture work (its bot was consistent but sat below this profile).
      max_point_loss: 26.0
      mistake_freq: 0.72
      policy_weight: 0.12
      randomness: 0.78
      random_move_chance: 0.06
      local_bias: 0.0
      local_bias_in_opening: false
      local_bias_from_candidates: false
      reading_rate: 0.08
      policy_temp: 2.3
      wide_root_noise: 0.7
      sample_lapse: 0.35
      sample_loss_cap: 6.0
      first_line_chance: 0.0
      visits: 16
      min_candidates: 10
      opening_moves: 3
      pass_threshold: 0.10
      clarity_prior: 0.87
      clarity_score_gap: 15.0
    "6k":
      # INTERPOLATED between the measured 9k and 3k — the one unvalidated
      # rung; check ordering vs both neighbors in the next series.
      max_point_loss: 25.0
      mistake_freq: 0.70
      policy_weight: 0.12
      randomness: 0.78
      random_move_chance: 0.045
      local_bias: 0.0
      local_bias_in_opening: false
      local_bias_from_candidates: false
      reading_rate: 0.21
      policy_temp: 1.95
      wide_root_noise: 0.58
      first_line_chance: 0.0
      visits: 16
      min_candidates: 10
      opening_moves: 3
      pass_threshold: 0.10
      clarity_prior: 0.86
      clarity_score_gap: 13.0
    "3k":
      # Ran as cand-6k in the tournament: histogram 58% near-opt / 9%
      # blunders ≈ human 3k-2k band; beat the 9k 5/6 (+32.7) at n=6.
      # ROUND 2b (2026-07-05): round-2 left 3k playing 1d/Boulder ~even
      # (compressed top). λ 0.15 -> 0.28 to weaken (miss the vital point
      # more often); nothing else touched. Re-tested vs 6k + 1d only.
      max_point_loss: 24.0
      mistake_freq: 0.68
      policy_weight: 0.12
      randomness: 0.78
      random_move_chance: 0.04
      local_bias: 0.0
      local_bias_in_opening: false
      local_bias_from_candidates: false
      reading_rate: 0.24
      policy_temp: 1.7
      wide_root_noise: 0.55
      sample_lapse: 0.28
      sample_loss_cap: 4.0
      first_line_chance: 0.0
      visits: 16
      min_candidates: 10
      opening_moves: 3
      pass_threshold: 0.10
      clarity_prior: 0.85
      clarity_score_gap: 12.0
    "1d":
      # = BOULDER (the old sampling 9k): played Patrick (IGS 2d ≈ OGS
      # 2-3d) dead even over 3 games at komi 6.5 — his call to seat it at
      # Void/1d on the OGS scale. The stronger rr .80/t1.0 config it
      # replaces is archived as a future beyond-Void bot.
      max_point_loss: 23.0
      mistake_freq: 0.63
      policy_weight: 0.20
      randomness: 0.71
      random_move_chance: 0.04
      local_bias: 0.0
      local_bias_in_opening: false
      local_bias_from_candidates: false
      reading_rate: 0.55
      policy_temp: 1.15
      wide_root_noise: 0.45
      first_line_chance: 0.0
      visits: 16
      min_candidates: 9
      opening_moves: 4
      pass_threshold: 0.10
      clarity_prior: 0.80
      clarity_score_gap: 10.0

  5x5:
    "30k":
      # b28 calibration round 1: b20-clone landed at 36% (margin -5.66) over
      # 50 games. Bumping visits 4 -> 8 to compensate for b28's flatter policy
      # distribution at low visit counts (the candidate list at 4 visits has
      # 12+ near-equal-prior moves on this board, so the mistake-injection
      # mechanism picks worse moves on average than b20 does at the same
      # nominal profile).
      max_point_loss: 38.0
      mistake_freq: 0.74
      policy_weight: 0.07
      randomness: 0.90
      random_move_chance: 0.22
      local_bias: 0.87
      local_bias_in_opening: true
      first_line_chance: 0.0
      visits: 8
      min_candidates: 10
      opening_moves: 2
      pass_threshold: 0.05
      clarity_prior: 1.1
      clarity_score_gap: 999.0
